{
  "title": "Events",
  "countBadge_one": "{{count}} shown",
  "countBadge_other": "{{count}} shown",
  "countBadgeFetching_one": "{{count}} shown…",
  "countBadgeFetching_other": "{{count}} shown…",
  "dedupeCollapsed_one": "{{count}} duplicate collapsed (UAC split-token)",
  "dedupeCollapsed_other": "{{count}} duplicates collapsed (UAC split-token)",
  "loading": "loading events…",
  "errorTitle": "Couldn't load events",
  "empty": {
    "title": "No events match",
    "body": "Widen the time window or clear pc / kind / source to see more rows. Agents emit events via the collect-winlog-events manifest; a freshly bootstrapped fleet may still be backfilling."
  },
  "filters": {
    "pcId": "pc",
    "kinds": "kind",
    "sources": "source",
    "chipHint": "(click: include → exclude → off)",
    "chipStates": {
      "off": "not filtered",
      "include": "included",
      "exclude": "excluded"
    },
    "payload": "payload (key = value)",
    "dedupe": "collapse duplicates",
    "forensicNote": "logon_type only exists on collect-winlog-logons-all data and pre-#378 rows (before the Winlogon source switch); duplicates there are UAC split-token pairs.",
    "since": "since",
    "limit": "limit",
    "placeholders": {
      "pcId": "exact match — eg. pc-01",
      "payloadKey": "key — eg. user",
      "payloadValue": "value — eg. yukimemi"
    },
    "sincePresets": {
      "1h": "last 1h",
      "24h": "last 24h",
      "7d": "last 7d",
      "30d": "last 30d",
      "all": "all time"
    }
  },
  "columns": {
    "when": "when",
    "pcId": "pc",
    "kind": "kind",
    "source": "source",
    "recordId": "record id",
    "payload": "payload"
  },
  "payload": {
    "show": "show"
  },
  "chart": {
    "title": "Per-PC timeline"
  },
  "opTimeline": {
    "title": "Operational state",
    "subtitle": "reconstructed from power / session / sleep / active events — gaps in a lane mean no event of that kind (e.g. powered off, or idle)",
    "lanes": {
      "power": "Power",
      "session": "Session",
      "sleep": "Sleep",
      "active": "Active / Idle"
    },
    "openStart": "started before this window",
    "openEnd": "still open at window end",
    "spanTooltip": "{{lane}}: {{from}} → {{to}}",
    "markerTooltip": "{{kind}} at {{at}}",
    "empty": "No power / session / sleep / active events in this window for this PC."
  },
  "heatmap": {
    "title": "PC × time activity",
    "bucketHint": "(bucket: {{bucket}})",
    "cellTooltip_one": "{{count}} event",
    "cellTooltip_other": "{{count}} events"
  },
  "chartPcCap": "showing busiest {{shown}} of {{total}} PCs — narrow the filters for the rest"
}
